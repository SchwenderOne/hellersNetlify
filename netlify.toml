[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Note: Redirects are handled by _redirects file in dist folder
# This ensures static assets (scripts, styles) are served correctly

